# 生产环境配置（vite build --mode production 时加载）
# 注：NODE_ENV 由 Vite 根据 mode 自动设置，无需在此声明。

# 后端接口基础地址。
# 生产环境没有 Vite 开发代理，因此必须写成完整后端地址。
# 当前生产地址与本地运行地址一致（后端 Spring Boot 运行在 localhost:8080），
# 所以直接指向 http://localhost:8080/api。
# 后续若部署到独立域名/服务器，只需把这里改成对应后端地址即可，例如：
#   VITE_API_BASE_URL='https://api.your-domain.com/api'
VITE_API_BASE_URL='http://localhost:8080/api'
